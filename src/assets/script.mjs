/**
 * - import `setupChart` from `src/assets/setup.mjs`
 * - use `setupChart` to receive the basic setup of axles, domain, scales for a basic chart (viewable in browser)
 * - Generate 50 random `Point[]` tuples between the provided min and maxes returned from `setupChart()`
 * - plot these points on the chart
 * - add a tooltip that displays on hover for each dot
 */
